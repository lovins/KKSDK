// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {
        jcenter()
        google()
        maven { url 'https://jitpack.io' }
        maven { url "https://s3.amazonaws.com/moat-sdk-builds" }
        maven { url 'https://maven.google.com'}
        maven { url 'https://dl.bintray.com/ironsource-mobile/android-sdk'}
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0'
        //classpath 'com.google.gms:google-services:4.1.0'
    }
}

allprojects {
    repositories {
        jcenter()
        google()
        flatDir {
            dirs 'libs'
        }
        mavenCentral()
        maven { url 'https://s3.amazonaws.com/moat-sdk-builds' }
        maven { url 'https://tapjoy.bintray.com/maven' }
        maven { url 'https://jitpack.io' }
        maven { url 'https://dl.bintray.com/ironsource-mobile/android-sdk'}
    }
}
apply plugin: 'com.android.application'
//apply plugin: 'com.google.gms.google-services'


dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
//    implementation 'com.facebook.android:facebook-login:[5,6)'
//    implementation 'com.facebook.android:facebook-core:[5,6)'
    implementation 'androidx.multidex:multidex:2.0.1'
    implementation project(':KKSDK')
    implementation project(':bytedanceadapter')
    implementation project(':yumiadapter')
    implementation project(':sigmobadapter')
    //ogury
//    implementation(name: 'presage', ext: 'aar')
//    implementation(name: 'mopubevents', ext: 'aar')
}

android {
    compileSdkVersion 28
    buildToolsVersion '28.0.2'
    compileOptions {
        sourceCompatibility 1.8
        targetCompatibility 1.8
    }
    defaultConfig {
        multiDexEnabled true
        minSdkVersion 16
        targetSdkVersion 28
        applicationId 'com.playmil.blockfit3d'
        ndk {
            abiFilters 'armeabi-v7a'
        }
        versionCode 1
        versionName '0.1'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress '.unity3d', '.ress', '.resource', '.obb'
    }

    signingConfigs {
        release {
            storeFile file('/Users/jaywin/Desktop/AndroidkeyStore/VoxelCN.keystore')
            storePassword 'appleboy927'
            keyAlias 'voxel'
            keyPassword 'appleboy927'
        }
        debug {
            storeFile file('/Users/jaywin/Desktop/AndroidkeyStore/VoxelCN.keystore')
            storePassword 'appleboy927'
            keyAlias 'voxel'
            keyPassword 'appleboy927'
        }
    }
    buildTypes {
        debug {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            jniDebuggable true
//            signingConfig signingConfigs.debug
        }
        release {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
//            signingConfig signingConfigs.debug
        }
    }
    packagingOptions {
        doNotStrip '*/armeabi-v7a/*.so'
        doNotStrip '*/x86/*.so'
    }

}
